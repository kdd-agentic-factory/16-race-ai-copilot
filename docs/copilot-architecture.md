# Copilot Architecture

The copilot receives conversational requests, classifies intent, selects a prompt and model profile, proposes tool calls, gathers evidence, applies safety checks, and returns a structured answer.
